if not customarathon then customarathon = {} end
if not customarathon.functions then customarathon.functions = {} end
if not customarathon.lists then customarathon.lists = {} end

require "lists.basic" 
require "lists.mod-support"
require "functions"
